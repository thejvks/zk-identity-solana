[package]
name = "zk-identity"
version = "0.1.0"
description = "Privacy-first ZK identity verification on Solana"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "zk_identity"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
